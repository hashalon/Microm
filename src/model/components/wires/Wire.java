package model.components.wires;

import model.Vector3;
import model.components.Block;
import model.grid.Grid;

public class Wire extends Block{

	public Wire(Grid grid, Vector3 position) {
		super(grid, position);
	}
	
}
